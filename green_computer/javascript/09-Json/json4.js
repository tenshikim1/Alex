

/** 걍 바로 안에 넣어도 된다. */
var circle = { 
    center: {
    x: 5,
    y: 10
    }
    , // 중심의 좌표
    radius: 5.10    //반지름
};

console.log("원의 중심 좌표: (%d, %d)", circle.center.x, circle.center.y);
console.log("원의 반지름: %d", circle.radius);